require('should');
// const MacysBooking = require('./macys_booking');

describe('My App', () => {
  it('should test something', (done) => {
    const x = 1;
    x.should.eql(1);
    done();
  });
});
